# sVault frontend — copy to .env.local. NEVER commit real values.

# Backend API base. On Vercel, /api/* routes to the Python function, so "/api/v1" works.
NEXT_PUBLIC_API_BASE=/api/v1

# Supabase (public) — added in M1
NEXT_PUBLIC_SUPABASE_URL=https://hgopttbpoyvmlzgzyzio.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
